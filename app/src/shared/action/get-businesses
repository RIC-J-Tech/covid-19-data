import {httpConfig} from "../utils/http-config";
import _ from "lodash";

	export const getBusinessByBusinessId =()=> async(dispatch) =>{

			const payload = await httpConfig(`/apis/business/${id}`)
			dispatch({type:"GET_BUSINESS_BY_BUSINESS_ID" ,payload: payload.data});

		};

export const getBusinessByBusinessName =()=> async(dispatch) =>{

	const payload = await httpConfig(`/apis/business/${businessName}`)
	dispatch({type:"GET_BUSINESS_BY_BUSINESS_NAME" ,payload: payload.data});

};